{
  "$schema": "https://json.schemastore.org/nx-project.json",
  "name": "booking-worker",
  "sourceRoot": "apps/booking-worker/src",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm -C apps/booking-worker dev"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm -C apps/booking-worker build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'no tests'"
      }
    }
  },
  "tags": ["type:worker"]
}
